/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // www.airobotphysical.com/robot-platform 경로로 편입 (메인 플랫폼이 프록시)
  basePath: "/robot-platform",
};

module.exports = nextConfig;
